import { DEFAULT_CODE } from './data.js';

export const createState = () => ({
  code: DEFAULT_CODE,
  theme: 'ray',
  padding: 'medium',
  windowStyle: 'mac',
  showLineNumbers: true,
  mode: 'dark',
  language: 'generic',
  resolution: 3
});
